
version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo Running tests...
      - echo "Tests passed"   # Replace with actual test command

artifacts:
  files:
    - '**/*'
